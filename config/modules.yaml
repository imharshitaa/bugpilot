modules:
  xss:
    enabled: true
    description: Reflected and basic injection-style XSS checks.
  sqli:
    enabled: true
    description: Error-signature SQL injection detection.
  ssrf:
    enabled: true
    description: Safe SSRF indicator checks using internal host payloads.
  auth_bypass:
    enabled: true
    description: Missing authentication and access control checks.
  misconfig:
    enabled: true
    description: Missing security header and baseline hardening checks.
  idor:
    enabled: true
    description: Object-level authorization checks by ID mutation.
  open_redirect:
    enabled: true
    description: Open redirect behavior checks.
  path_traversal:
    enabled: true
    description: Directory traversal indicator checks.
  file_inclusion_indicator:
    enabled: true
    description: File inclusion error-pattern checks.
  cors_misconfig:
    enabled: true
    description: CORS allow-origin misconfiguration checks.
  csrf:
    enabled: true
    description: Missing CSRF token checks in forms.
  rate_limit_bruteforce:
    enabled: true
    description: Rate limiting and brute-force resistance checks.
  jwt_validation_weaknesses:
    enabled: true
    description: Weak JWT validation and insecure token handling checks.
  insecure_deserialization:
    enabled: true
    description: Insecure deserialization error-pattern checks.
  business_logic_abuse:
    enabled: true
    description: Business workflow and tampering abuse indicators.
  graphql_specific_testing:
    enabled: true
    description: GraphQL introspection and endpoint exposure checks.
  file_upload_testing:
    enabled: true
    description: File upload validation and restriction weakness checks.
  xxe:
    enabled: true
    description: XML External Entity parsing indicator checks.
